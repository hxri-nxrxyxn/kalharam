version: '3.8'

services:
  web:
    build: 
      context: .
    ports:
      - "5173:5173"
    environment:
      - PORT=5173
    volumes:
      - uploads_data:/app/web/static/assets/uploads
    restart: always

volumes:
  uploads_data:
